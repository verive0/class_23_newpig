1、结合业务说说你用python怎么做接口自动化（python +requests+pytest）我用Python+requests+pytest实现接口自动化的。在我之前的项目里，我实现了用户增删查改的接口自动化，结合po模式将整个项目分成公共类模块，配置文件模块，工具类模块，日志模块，测试报告等。比如说用户新增，我先定义一个类对数据库进行连接，实例化requests中的Session类传入url、请求头和请求体，实现登录，随后调用这个类参数传入新增用户所需要的入参，比如客户名称，客户电话，公司地址等，我们公司用的是json格式在接口里传参，所以用json格式将入参传入新增用户的接口，拿到接口的响应体断言，我是直接断言业务状态码为0000，失败就报错，方便之后定位问题。再比如说查询用户，同样调用这个session类参数保持同一会话，在查询接口中传入我所想查询的客户信息，对应好参数，比如username对应客户名，userlevel对应客户等级，把所需要的参数传入后拿到响应体返回的客户列表，断言是否有我需要找的客户。就这些2、了解dubbo接口3、jmeter做接口测试，对动态参数的操作